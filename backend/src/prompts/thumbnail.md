# Thumbnail — v1

Consumed by: Image Agent (thumbnail pass)
Placeholders: `{{thumbnailPrompt}}`, `{{styleGuide}}`, `{{width}}`, `{{height}}`

---

Positive prompt:

`{{thumbnailPrompt}}`, `{{styleGuide}}`, single clear subject, strong contrast,
uncluttered background, vertical composition `{{width}}`x`{{height}}`.

Negative prompt:

text, watermark, logo, caption, busy background, multiple subjects,
low resolution, blurry.
